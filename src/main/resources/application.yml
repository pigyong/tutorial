spring:
  application:
    name: demo
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/novice_tutorial?serverTimezone=UTC&characterEncoding=utf8&useUnicode=true&useSSL=false
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  type-aliases-package: com.smil.dao
  mapper-locations: classpath:mappers/*.xml
server:
  port: 80

